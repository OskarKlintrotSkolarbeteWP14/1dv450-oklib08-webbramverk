var C = _.assign({}, S, {
  //App
  appName: 'clientApp', // Don't forget that this must be the same as in index.html!
  // API
  trustedDomain: 'http://localhost:8080/',
  apiURL: 'http://localhost:8080/api/v1/',
  baseURL: 'http://localhost:8080/',
  basicURL: 'localhost:8080/api/v1/',
  // sessionStorage
  USER_INFO: 'userInfo'
})
